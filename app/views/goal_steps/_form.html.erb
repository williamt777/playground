<%= form_with(model: goal_step, local: true) do |form| %>
  <% if goal_step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(goal_step.errors.count, "error") %> prohibited this goal_step from being saved:</h2>

      <ul>
        <% goal_step.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
